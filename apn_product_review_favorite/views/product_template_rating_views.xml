<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================================================
         VISTAS PARA PRODUCT.TEMPLATE (PLANTILLAS)
    ================================================= -->

    <!-- Vista Lista (tree) para product.template -->
    <record id="view_product_template_rating_list" model="ir.ui.view">
        <field name="name">product.template.rating.tree</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <list string="Top Rated Products" default_order="vote_average desc, vote_count desc"
                  create="false" edit="false" delete="false">
                <field name="name" string="Product" width="100"/>
                <field name="favorite_count" width="100"/>
                <field name="comment_count" width="100"/>
                <field name="vote_count" string="Votes" width="100"/>
                <field name="vote_average" widget="float" string="Avg. Rating" width="100"/>
            </list>
        </field>
    </record>

    <!-- Vista Kanban para product.template -->
    <record id="view_product_template_rating_kanban" model="ir.ui.view">
        <field name="name">product.template.rating.kanban</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_product_rating" default_order="vote_average desc, vote_count desc"
                    create="false" edit="false" delete="false">
                <field name="name"/>
                <field name="vote_count"/>
                <field name="vote_average"/>
                <field name="comment_count"/>
                <field name="image_1920"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('product.template', 'image_1920', record.id.raw_value)"
                                     alt="Product Image" class="img img-fluid"/>
                            </div>
                            <div class="o_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                                <!-- Contenedor flex: estrellas y comentarios en la misma línea -->
                                <div class="d-flex justify-content-between align-items-center mt-1">
                                    <!-- Grupo izquierdo: estrellas y contadores -->
                                    <div>
                                        <!-- Siempre 5 estrellas: llenas según floor(vote_average), vacías el resto -->
                                        <t t-foreach="Array.from({length: 5}, (_, i) => i)" t-as="i">
                                            <i t-attf-class="fa fa-star{{ i &lt; Math.floor(record.vote_average.raw_value || 0) ? '' : '-o' }} text-primary ms-1"/>
                                        </t>
                                        <span class="badge bg-primary ms-1">
                                            <field name="vote_average" widget="float"/>
                                        </span>
                                        <span class="text-muted ms-2">
                                            (<field name="vote_count"/> votes)
                                        </span>
                                    </div>
                                    <!-- Grupo derecho: favoritos a la izquierda de comentarios -->
                                    <div class="d-flex align-items-center">
                                        <span class="text-muted small me-3">
                                            <i class="fa fa-heart text-danger me-1"/>
                                            <field name="favorite_count"/>
                                        </span>
                                        <span class="text-muted small">
                                            <i class="fa fa-comments me-1"/>
                                            <field name="comment_count"/>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_product_template_rating_form" model="ir.ui.view">
        <field name="name">product.template.rating.form</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <form create="false" edit="false" delete="false" readonly="1">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="vote_average" widget="float" readonly="1"/>
                            <field name="vote_count" readonly="1"/>
                            <field name="comment_count" readonly="1"/>
                            <field name="favorite_count" readonly="1"/>
                        </group>
                        <group>
                            <!-- Imagen del producto -->
                            <field nolabel="1"  name="image_1920" widget="image" class="oe_avatar" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Ratings &amp; Comments">
                            <!-- Lista de valoraciones con estrellas, cliente y comentario -->
                            <field name="rating_ids" readonly="1" nolabel="1">
                                <list create="false" edit="false" delete="false" editable="bottom">
                                    <field name="partner_id" readonly="1"/>
                                    <field name="rating" widget="rating" readonly="1"/>
                                    <field name="feedback" readonly="1"/>
                                    <field name="create_date" readonly="1"/>
                                </list>
                            </field>
                        </page>

                        <!-- Nueva página de clientes favoritos -->
                        <page string="Favorite Customers">
                            <field name="favorite_partner_ids" readonly="1" nolabel="1">
                                <list create="false" edit="false" delete="false">
                                    <field name="name" readonly="1"/>
                                    <field name="email" readonly="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción de ventana para product.template top ratings -->
    <record id="action_product_template_ratings" model="ir.actions.act_window">
        <field name="name">Top Rated Templates Products</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="view_id" ref="view_product_template_rating_kanban"/>
        <field name="view_ids" eval="[ (5, 0, 0),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_product_template_rating_kanban')}),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_product_template_rating_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_product_template_rating_form')})]"/>
        <field name="search_view_id" ref="product.product_template_search_view"/>
        <field name="context">{'create': False}</field>
        <field name="domain">['|', '|', ('vote_count', '>', 0), ('favorite_count', '>', 0), ('comment_count', '>', 0)]</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent">
                No rated template products found.
            </p>
        </field>
    </record>
</odoo>